import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export const Timer = () => {
  return (
    <View>
      <Text>Timer</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
